<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="BookingService.MVVM.Views.BookingsAdmin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BookingsAdmin" Height="450" Width="700" WindowStartupLocation="CenterScreen">
    <Grid Margin="12">
        <StackPanel>
            <Label Content="Bookings"/>
            <DataGrid ItemsSource="{Binding Bookings}" AutoGenerateColumns="False" CanUserAddRows="False" Margin="0,8,0,8">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                    <DataGridTextColumn Header="User" Binding="{Binding User.Username}" Width="*"/>
                    <DataGridTextColumn Header="Room" Binding="{Binding Room.Name}" Width="*"/>
                    <DataGridTextColumn Header="Price" Binding="{Binding TotalPrice, StringFormat={}{0:C}}" Width="100"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="120"/>
                    <DataGridTemplateColumn Header="Actions" Width="160">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="Confirm" Command="{Binding DataContext.ConfirmCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" Margin="4"/>
                                    <Button Content="Cancel" Command="{Binding DataContext.CancelCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" Margin="4"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
            <!--<Button Content="Close" Width="120" HorizontalAlignment="Center" Margin="0,8,0,0" Click="Close_Click"/> -->
        </StackPanel>
    </Grid>
</Window>